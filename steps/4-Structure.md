src/
 ├── app/              ← app bootstrap      mkdir src\app
 ├── routes/           ← router config      mkdir src\routes
 ├── components/       ← shared UI          mkdir src\components
 ├── features/         ← domain features    mkdir src\features
 │    ├── products/                         mkdir src\features\products
 │    ├── cart/                             mkdir src\features\cart
 │    ├── auth/                             mkdir src\features\auth
 │    └── checkout/                         mkdir src\features\checkout
 ├── store/            ← redux + saga root          mkdir src\store
 ├── services/         ← REST + GraphQL clients     mkdir src\services
 ├── hooks/            ← custom hooks               mkdir src\hooks
 ├── types/            ← TS models                  mkdir src\types
 ├── utils/            ← helpers                    mkdir src\utils
 ├── styles/                                        mkdir src\styles
 ├── assets/                                        mkdir src\assets
 ├── contexts/         ← React Context              mkdir src\contexts
 ├── hoc/              ← HOC pattern                mkdir src\hoc
 ├── renderProps/      ← render-prop examples       mkdir src\renderProps
 └── stories/          ← Storybook                  mkdir src\stories


